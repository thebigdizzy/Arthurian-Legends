using UnityEngine;
using System.Collections;

public class treeDoors : MonoBehaviour {



	// event function used to get rid of the tree
	void onEvent(){
		gameObject.SetActive (false);
	}

	// Use this for initialization
	void Start () {
	
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
